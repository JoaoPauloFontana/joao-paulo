{
  "private": true,
  "name": "vulmix-app",
  "scripts": {
    "build": "mix",
    "dev": "mix watch",
    "prod": "mix --production",
    "serve": "npx http-server -p 8000 -a localhost _dist --gzip --proxy http://localhost:8000?"
  },
  "dependencies": {
    "animate.css": "^4.1.1",
    "aos": "^3.0.0-beta.6",
    "vulmix": "^0.6.0-beta.preview.3"
  }
}
